name: drone_delivery
description: Application de gestion des opérations de drone en situation de crise
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI & Navigation
  cupertino_icons: ^1.0.6
  
  # HTTP & API
  http: ^1.1.0
  
  # State Management
  provider: ^6.1.1
  
  # Charts & Data Visualization
  fl_chart: ^0.65.0
  
  # Utils
  shared_preferences: ^2.2.2
  
  # Audio for voice commands
  speech_to_text: ^6.6.0
  permission_handler: ^11.2.0
  
  # Fonts
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

